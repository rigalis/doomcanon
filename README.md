# doomcanon

Building a heterogeneous Kubernetes cluster across x86_64 and ARM64,
running the same recorded Doom demo on both architectures, and comparing
the per-tic game state to verify the simulation is truly
deterministic regardless of CPU.

Built to learn Kubernetes, Go, gRPC, and containerization by running a real cross-architecture experiment.